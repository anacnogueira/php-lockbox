services:
  # Serviço do PHP
  app:
    image: php:8.5-fpm-alpine
    container_name: lockbox
    volumes:
      - ./src:/var/www/html
    restart: unless-stopped

  # Serviço do Nginx
  web:
    image: nginx:alpine
    container_name: nginx-server-lockbox
    ports:
      - "80:80"
    volumes:
      - ./src:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    restart: unless-stopped
